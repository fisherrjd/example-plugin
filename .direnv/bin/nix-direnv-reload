#!/usr/bin/env bash
set -e
if [[ ! -d "/home/jade/github/projects/example-plugin" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/jade/github/projects/example-plugin")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/jade/github/projects/example-plugin" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/jade/github/projects/example-plugin/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/jade/github/projects/example-plugin/.envrc" "/home/jade/github/projects/example-plugin/.direnv"/*.rc
